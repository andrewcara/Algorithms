class Solution:
    def minReorder(self, n: int, connections: list[list[int]]) -> int:
        
        last_city = connections[0]
        flipped = 0
    
            
            
                
        
        
        return flipped
